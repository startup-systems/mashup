language: node_js
node_js:
  - "6"
branches:
  only:
    # needs to be listed explicitly
    - gh-pages
    - /.*/
before_install: npm install -g grunt-cli
sudo: false
cache:
  directories:
    - node_modules
